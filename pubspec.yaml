name: flutter_ci_tools
description: Reusable CI tooling for Flutter apps — build orchestration, git versioning, deploy services, and structured logging.
version: 0.0.3
publish_to: https://pub.dev
repository: https://github.com/sunbird89629/flutter_ci_tools
topics:
  - ci
  - flutter
  - build
  - deploy
  - devops

environment:
  sdk: ^3.4.0

dev_dependencies:
  test: ^1.25.0
